# FileUpload Type
scalar FileUpload
scalar ObjectID

type Query{
    test: String!
}

type Mutation{
    imgUpload(file: FileUpload): Boolean!

    register(
        email: String!,
        password: String!,
        age: Int!,
        gender: Int!
    ): User!

    login(
        email: String!,
        password: String!
    ): String!
}

type User{
    email: String!
    age: Int!
    gender: Int!
    _id: ObjectID!
}